// called when an image is clicked on from the gallery
// Redierects to staging with the selected image passed as input

	
$(document).ready(function(){
	$( ".row" ).click(function() {
		//alert($('.row img:nth-child(1)').attr('src'));
	});
});